<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Adaptive-icon background — the dusk-seam dark that the bloom
         foreground sits on. The system masks this rectangle to whatever
         shape the launcher prefers (circle, squircle, rounded square),
         so a solid colour is enough; the foreground PNG carries the
         actual artwork. -->
    <color name="ic_launcher_background">#050507</color>
</resources>
