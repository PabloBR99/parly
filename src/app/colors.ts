/** Shared color constants — single source of truth for the Parly palette. */

/** Primary background — pure black. The phone becomes a void. */
export const BG_DEEP = '#000000';

/** Pure white for main text. */
export const TEXT_PRIMARY = 'rgba(255, 255, 255, 0.92)';

/** Supporting text — translation / original language line. */
export const TEXT_SECONDARY = 'rgba(255, 255, 255, 0.38)';

/** Ghost text — previous exchange, barely visible. */
export const TEXT_GHOST = 'rgba(255, 255, 255, 0.12)';

/** Muted labels and hints. */
export const TEXT_MUTED = 'rgba(255, 255, 255, 0.20)';

/** Ambient divider glow at rest. */
export const DIVIDER_GLOW = 'rgba(255, 255, 255, 0.06)';
