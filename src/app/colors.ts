/** Shared color constants — single source of truth for the Parly palette. */

/** Primary background — deep charcoal, not pure black. */
export const BG_DEEP = '#0a0a0a';

/** Warm white for main text (slightly cream, not cold). */
export const TEXT_PRIMARY = 'rgba(255, 253, 247, 0.92)';

/** Supporting text — translation / original language line. */
export const TEXT_SECONDARY = 'rgba(255, 253, 247, 0.38)';

/** Ghost text — previous exchange, barely visible. */
export const TEXT_GHOST = 'rgba(255, 253, 247, 0.12)';

/** Muted labels and hints. */
export const TEXT_MUTED = 'rgba(255, 253, 247, 0.20)';

/** Ambient divider glow at rest. */
export const DIVIDER_GLOW = 'rgba(255, 253, 247, 0.06)';
